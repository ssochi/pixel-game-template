{
  "name": "pixel-game-template",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "2D top-down pixel-art game template — procedural asset test scene",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "vite": "^5.4.10"
  }
}
